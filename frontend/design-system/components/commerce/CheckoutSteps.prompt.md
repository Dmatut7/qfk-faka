Progress stepper for the four-stage buy journey.

```jsx
<CheckoutSteps current={2} /> {/* on the payment page */}
```

Props: `steps` (default 选购/下单/付款/取卡), `current` (0-based). Done steps show a check, current is blue.
