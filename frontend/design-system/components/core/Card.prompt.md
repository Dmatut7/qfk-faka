Surface container for product cards, order summaries, payment panels.

```jsx
<Card interactive>…product…</Card>
<Card elevation="raised" pad>…order summary…</Card>
```

Props: `pad` (default true), `interactive` (hover-lift, for clickable cards), `elevation` (`flat | sm | raised`), `as`.
