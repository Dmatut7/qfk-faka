# QFK 发卡系统 — 开发任务清单 (tasks)

> 规则(见 CLAUDE.md):每个任务 实现 → `composer test` 全绿 → 勾选 → `git commit`。
> 金额 / 卡密发放 / 支付回调相关:**先写测试再写实现**。
> 任务粒度尽量小、可独立测试。`★` 标记的为核心模块停顿验收点。

图例:`[ ]` 待办 · `[x]` 完成 · `(TDD)` 必须测试先行。

---

## M0 — 项目骨架与测试框架 ✅(第一阶段已完成)
- [x] **T0.1** ThinkPHP 6.1 骨架 + Composer(think-orm / think-migration / phpunit ^9)。
  - 验收:`php think version` 正常;`composer install` 通过。
- [x] **T0.2** 双库与环境:创建 `qfk` / `qfk_test`,`.env`(dev)`.env.testing`(test,socket 连接)。
  - 验收:`php think migrate:status` 能连库;测试以 `qfk_test` 运行且有库名护栏。
- [x] **T0.3** 测试框架:`phpunit.xml` + `tests/bootstrap.php`(testing 环境、自动迁移、库名护栏)+ `tests/TestCase.php`(事务回滚隔离 + 模拟 HTTP)。
  - 验收:`composer test` 可运行。
- [x] **T0.4** 健康检查接口 `GET /health` + 集成测试。
  - 验收:返回 `{code:0,data:{status:ok,database:ok}}`,HTTP 200;`HealthTest` 通过。

---

## M1 — 通用基建(响应/异常/常量/基类)
- [ ] **T1.1** 统一响应与错误码:`app/common.php` 助手 `apiSuccess($data)`/`apiError($code,$msg)`、错误码常量类 `app\common\Code`。
  - 验收:单元测试断言响应结构 `{code,msg,data}` 与各错误码常量值。
- [ ] **T1.2** 全局异常处理:验证异常 → 422+1001;业务异常 `BizException($code,$msg)` → 统一 JSON;404/500 统一格式。
  - 验收:Feature 测试:抛 `BizException` 返回对应 code;非法路由返回统一 404 JSON。
- [ ] **T1.3** API 基类控制器 `BaseApiController` + 请求参数获取/校验封装。
  - 验收:子类继承可直接返回成功/失败;含一个示例路由的测试。
- [ ] **T1.4** 工具:`OrderNo` 生成器(唯一)、`Money`(bcmath 封装 add/sub/mul/cmp,scale=2)、`Token`(生成+哈希)。(TDD)
  - 验收:`Money` 单元测试覆盖加减乘与比较的精度;`OrderNo` 唯一性(批量生成无重复);`Token` 哈希可校验。

## M2 — 数据迁移与模型(基础表)
- [ ] **T2.1** 迁移+模型:`system_settings`、`admins`。
  - 验收:`migrate:run` 建表成功;模型 CRUD 测试通过;唯一索引生效(重复 key/username 抛错)。
- [ ] **T2.2** 迁移+模型:`merchants`(含 balance/commission 等字段、唯一索引)。
  - 验收:建表与索引正确;模型测试:唯一 username/slug 冲突被拒。
- [ ] **T2.3** 迁移+模型:`categories`、`products`(外键、索引)。
  - 验收:外键约束生效(删商户级联);`uniq_merchant_sku` 生效。
- [ ] **T2.4** 迁移+模型:`cards`(取卡索引 `idx_pick`、`uniq_secret`、状态机常量)。
  - 验收:建表;`uniq(product_id,secret_hash)` 去重生效;状态常量定义。
- [ ] **T2.5** 迁移+模型:`buyers`、`orders`(索引、外键、状态常量)。
  - 验收:建表;`uniq_order_no` 生效;`idx_expire` 存在。
- [ ] **T2.6** 迁移+模型:`payments`、`payment_channels`、`merchant_fund_logs`、`withdrawals`、`access_tokens`。
  - 验收:建表;`uniq(channel,channel_trade_no)`、`uniq_token` 等关键唯一索引生效。

## M3 — 鉴权
- [ ] **T3.1** Token 服务:签发(存哈希+过期)、校验、撤销。(TDD)
  - 验收:签发后可校验;过期/撤销后失效;错误 token 拒绝。
- [ ] **T3.2** 平台管理员登录/登出 + `AdminAuth` 中间件。
  - 验收:正确密码登录得 token;错误密码 2001;受保护路由无 token 返回 401。
- [ ] **T3.3** 商户登录/登出 + `MerchantAuth` 中间件;冻结商户拒绝登录。
  - 验收:同上;`status≠1` 商户登录被拒;越权访问他人资源 403。
- [ ] **T3.4** 平台创建商户 / 商户初始账号 + 改密。
  - 验收:管理员可建商户;商户改密后旧密码失效。

## M4 — 商品与卡密管理(商户后台)
- [ ] **T4.1** 分类 CRUD(商户级,鉴权+归属校验)。
  - 验收:增删改查正常;操作他人分类 403。
- [ ] **T4.2** 商品 CRUD + 上下架 + 限购字段。
  - 验收:CRUD/上下架可用;字段校验(price>0、min/max 合法)。
- [ ] **T4.3** 卡密批量导入(文本分行)、列表筛选、作废、删除未售。(TDD: 去重与计数)
  - 验收:导入 N 行得 N 张未售卡;重复卡密按 `uniq_secret` 跳过并报告;`products.stock` 同步;作废/删除仅作用于未售卡。

## M5 — 买家前台浏览与下单(发卡核心) ★停顿点 1
- [ ] **T5.1** 商店与商品浏览:`GET /s/{slug}` 商品列表、`GET /buyer/product/{id}` 详情(仅在售)。
  - 验收:仅展示在售商品与正确库存;下架商品详情 404/下架提示。
- [ ] **T5.2 (TDD)** 下单 + **并发安全预占卡密**:`OrderService::create()`,事务 + `FOR UPDATE SKIP LOCKED` 取卡,limit/库存/限购校验,写订单(expire_at),卡 `0→1`。
  - 验收:正常下单返回 `order_no`,卡被锁定且 `order_id` 正确;库存不足报 3002;超限购报 3003;金额用 bcmath 校验。
- [ ] **T5.3 (TDD) 并发测试**:多连接对"仅剩 M 张"商品并发下单。
  - 验收:恰好 M 单成功、各占不同卡、其余库存不足;无任何卡被两单占用;`已锁定+已售 = 初始库存`。
- [ ] **T5.4** 订单超时回收:`order:clean` 命令/任务,事务内关单 + 释放锁定卡(`1→0`)。
  - 验收:构造过期未付单执行后 → 订单关闭、卡回到未售、stock 回补;并发释放安全。
- [ ] **T5.5** 买家订单查询:`order_no + 邮箱` 查状态与已购卡密(未发货不泄露)。
  - 验收:已发货返回卡密;待支付不返回卡密;邮箱不符 403/404。

> **★ 停顿验收点 1 — 卡密发放模块**:运行全部测试(含并发测试)全绿后停止,等待人工验收。

## M6 — 支付对接(渠道抽象 + 回调) ★停顿点 2
- [ ] **T6.1** 渠道抽象:`PayDriverInterface`(buildPay/verify/parse/success/fail)+ `PayManager`(按 code 取驱动+配置)。
  - 验收:接口与管理器单元测试;未知/停用渠道报 5002。
- [ ] **T6.2 (TDD)** 首个驱动:易支付/MD5 `EpayDriver`,实现 `buildPay`(签名)+ `verify`(验签)+ `parse`。
  - 验收:**验签测试**:构造正确签名验签通过;错误/缺失签名拒绝;签名算法与字段排序正确。
- [ ] **T6.3** 发起支付:`POST /buyer/order/{no}/pay` → 校验订单待支付+未过期 → 写 `payments`(待支付)→ 返回跳转/二维码参数。
  - 验收:待支付订单可发起;已支付/已关闭/过期订单被拒(4002/4003);生成唯一 `payment_no`。
- [ ] **T6.4 (TDD)** 回调入口 `POST /pay/notify/{channel}`:验签 → 定位支付单 → 金额校验 → 订单行锁幂等 → 原子发货 + 结算 → 正确应答。
  - 验收:成功回调 → 订单已发货、卡 `1→2`、`delivered_content` 写入、商户余额按佣金入账、流水两条、返回成功应答。
- [ ] **T6.5 (TDD) 回调安全专项**:
  - 验签失败 → 不改任何状态、返回失败应答(5001)。
  - **幂等**:同一成功回调投递两次 → 仅发货一次、仅结算一次、卡密不重复消耗、第二次直接成功应答(5003)。
  - **金额篡改**:回调金额≠订单金额 → 拒绝发货并告警(4004)。
  - 验收:以上三类断言全部通过。
- [ ] **T6.6** 端到端发货链路 Feature 测试:下单 → 发起支付 → 模拟回调 → 前台查询拿到卡密。
  - 验收:全链路一次走通,状态与金额一致。

> **★ 停顿验收点 2 — 支付对接模块**:运行全部测试(含验签/幂等/金额篡改)全绿后停止,等待人工验收。

## M7 — 商户后台:订单/资金/统计
- [ ] **T7.1** 订单列表/详情/筛选(归属校验)+ 手动关闭/补发。
  - 验收:仅见本商户订单;补发走发卡服务并记录。
- [ ] **T7.2** 资金:余额、流水分页、申请提现(校验余额、冻结)、提现记录。
  - 验收:提现冻结金额正确(balance→frozen,bcmath);余额不足被拒。
- [ ] **T7.3** 统计:销售额/订单数/热销商品(按时间区间)。
  - 验收:统计数与构造数据一致。

## M8 — 平台后台
- [ ] **T8.1** 商户管理:审核/冻结/解冻/改抽佣/重置密码/搜索。
  - 验收:状态流转正确;冻结后该商户登录被拒。
- [ ] **T8.2** 支付渠道管理:增删改、配置密钥、启停、验签自测。
  - 验收:渠道 CRUD;停用后下单端不可选、回调端拒绝。
- [ ] **T8.3** 提现审核:通过/拒绝/打款,资金回滚(拒绝时解冻)。
  - 验收:通过则扣冻结、记流水;拒绝则解冻回余额(bcmath 一致)。
- [ ] **T8.4** 平台配置 `system_settings` 读写;跨商户订单/佣金对账报表。
  - 验收:配置读写生效;报表数额与流水一致。

## M9 — 收尾与加固
- [ ] **T9.1** 全局中间件:CORS、请求日志、基础限流(下单/回调)。
- [ ] **T9.2** 库存/资金对账命令:`cards` 与 `products.stock`、`fund_logs` 与 `balance` 一致性校验。
- [ ] **T9.3** 种子数据:默认管理员、示例渠道、演示商户/商品/卡密。
- [ ] **T9.4** 文档:README 启动/部署/测试说明;API 简表。
- [ ] **T9.5** 全量测试套件运行并出报告(覆盖核心:发卡并发、回调验签/幂等/金额)。

---

### 进度备注
- 当前阶段:第一阶段(规划 + 骨架)已完成,等待进入第二/三阶段。
- 阻塞问题集中记录在 `docs/blockers.md`。
