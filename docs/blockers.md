# Blockers — 阻塞与待澄清问题

> 任务卡住、需求不明确、spec 与现实冲突时记录于此并**停止**,等待人工澄清。
> 每条包含:日期、任务号、卡点描述、已尝试方案、备选方案/建议、需要的决策。

格式模板:
```
## [YYYY-MM-DD] Txx — 一句话标题
- 背景:
- 卡点:
- 已尝试:
- 备选方案:
- 需要你定:
```

---

(当前无阻塞)

---

## 待澄清的产品决策(非阻塞,已采用默认值,可随时调整)

> 以下为第一阶段规划 + 对抗式设计评审后做出的默认取舍,已写入 spec(§10.5)。如需调整请告知,否则按默认推进;核心模块(M5/M6)有人工验收点可届时校正。

**架构与流程默认:**
1. **多商户 SaaS 模式**:平台运营、商户入驻。默认成立。
2. **买家游客下单**:支持游客(邮箱+联系方式),非强制账号。
3. **发货时机**:下单即预占卡密(锁 15min),支付成功转已售,超时释放。
4. **金额**:`DECIMAL(10,2)` + bcmath(元)。
5. **结算**:支付成功即按 `commission_rate` 入商户余额,商户提现、平台审核。
6. **并发取卡**:`FOR UPDATE SKIP LOCKED`(本机 MySQL 9.5 支持)。

**评审后需你拍板的范围决策(已先取保守默认,先做核心):**
| # | 决策点 | 采用的默认 | 若要改 |
|---|--------|-----------|--------|
| A | 支付渠道范围 | 仅易支付/MD5;支付宝/微信预留接口 | ✅ 用户已确认(2026-06-21):按默认,支付宝/微信先不实做 |
| B | **退款流程** | 本期不实现资金/卡密退款编排;异常订单入 `4005` 转人工;枚举占位保留 | 告知是否需要退款及卡密是否回收 |
| C | 发货通知(邮件/站内) | 本期预留接口、不实现投递 | 告知需要的通知渠道/模板 |
| D | 买家账号体系 | 默认游客;buyer 登录/订单历史延后 | 告知是否需要买家注册登录 |
| E | 每买家累计限购 | `max_buy`=单笔上限;不做累计限购 | 告知限购语义 |
| F | type=2 手动发货商品 | 本期主线只做 type=1 自动发卡 | 告知是否需要手动发货商品 |
| G | 卡密加密存储 | 明文 + secret_hash 去重 | 告知是否强制加密 |
| H | 超时关闭后又支付成功 | 转 `4005` 异常待人工/退款,返回 success 停重试 | 告知是否要自动补货/自动退款 |
| I | 删除策略 | 有交易记录的商户/商品禁止硬删,只冻结/下架 | 告知是否需要软删字段 |

> 以上均不阻塞 M1–M4 基建实现;按默认推进,到 M5/M6 停顿验收时可一并确认。

## [2026-06-21] 待 owner 验收:提现审核 / 对账(前端)
- 范围:平台后台 A4 提现审核(`/admin/withdrawals` 通过=扣冻结打款 / 拒绝=解冻退回)、A5 对账报表(`/admin/reports/settlement` 销售额/佣金/净额)。
- 状态:页面已接真接口、渲染通过、金额用 Money 两位小数;按指示**记录待 owner 验收,不阻塞**,已继续其余任务。
- 待确认:① 提现审批后资金回滚账目;② 对账汇总口径(净额=销售额−佣金)是否符合预期;后端 reject 暂未落库拒绝原因、settlement 未返回店铺名/订单数(仅 merchant_id),如需可扩展后端。
- 验收方式:见 `frontend/app/e2e/console-money.mjs`(提现申请→审批全流程)与 console 页 admin 区。

## [2026-06-22] 待 owner:已成功支付但订单已关闭(CLOSED→EXCEPTION)的对账/退款闭环
- 背景:`NotifyService` 在收到合法支付回调时,若订单已 CLOSED(超时关闭后买家又付款成功),按保守策略转 `EXCEPTION` 转人工,**不自动入账、不自动退款、不自动补货**(逻辑本次未改,谨慎转人工是对的)。
- 卡点:这类「平台已实际收款但订单关闭」的资金需要一个明确闭环:对账识别 → 退款 or 补货发卡 二选一 → 账目/流水落地。当前仅停留在 EXCEPTION 状态,缺少后续编排。
- 已尝试:维持现状(不改逻辑),仅记录备忘,避免静默错账。
- 备选方案:① 自动原路退款(需接退款接口,见决策 B/H);② 自动补货并正常入账结算;③ 保留人工台账由运营逐单处理。
- 需要你定:该场景走退款还是补货?是否需要后端退款接口与对账报表新增「已收款未入账」口径?

## [2026-06-22] 待 owner:查单 / 支付发起限流(需先确认不影响并发测试)
- 背景:R19 安全加固已给 `admin/login`、`merchant/login` 挂 IP 限流(10/分,固定窗口)。
- 卡点:`buyer/order/query`(查单)与 `buyer/order/:no/pay`、`pay/notify/:channel`(支付发起/回调)暂未加/未收紧限流——并发发卡与回调测试(M5.3/M6.5)会高频调用同一端点,直接挂 IP 限流易误伤测试并掩盖真实并发问题。
- 已尝试:仅对登录端点限流;查单/支付发起保持现状(回调已有宽松 120/分)。
- 备选方案:① 为查单/支付发起加按订单号或买家维度的限流(而非纯 IP),绕开并发测试的同 IP 高频;② 测试环境放宽阈值或跳过限流中间件;③ 维持现状由网关层(nginx/WAF)兜底。
- 需要你定:是否需要给查单/支付发起加限流?若加,用何种维度与阈值以避免影响 M5.3/M6.5 并发测试?
