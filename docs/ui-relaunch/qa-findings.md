# UI 落地 · 对抗式审查发现与修复追踪

> 来源:两轮 Opus 审查(视觉审查 4 agent + 深度审查 11 agent,移动+桌面 30+ 屏 vs design-brief §7 理想清单 + Kit)+ 逻辑回归审查 + 人工复核。截图证据:`frontend/app/e2e/{audit,newui,mobile}/`。

## ✅ 已修复并复验(本 session,已提交 ui-orange-relaunch 分支)

**显示 bug(影响可用性):**
- DataTable 操作列按钮被裁成 1px(商户审核/邀请码/内容管理)— 根因修一处惠及全后台
- admin 商品(跨商户)误用不存在的 `--color-*` 变量(零库存红警失效/图标不渲染)
- 分类图破图框 → background-cover 兜底
- 移动端顶栏塌陷(店名/简介与导航重叠)→ 响应式收纳
- 详情底部留白不足(并查明 fullPage "重叠"是截图伪影,视口实测无遮挡)

**功能补全 / 缺失:**
- 内容管理新增 资讯/常见问题/单页 三类 CRUD(对接后端 admin.Articles)— 端到端实测写入成功
- ProductDetail:hooks 违例除雷 + 起购限购提示 + 移除券入口 + 章节加载失败态
- StorefrontHome:筛选栏吸顶 + 底部 tab 去死按钮(回顶/滚动/切上新/客服)+ 安全区 + 桌面网格
- OrderLookup:下载链绝对/相对修正 + 桌面放宽 + 错误文案随凭证模式 + 风险提示兜底 + 投诉多行
- Portal:图标去重 + 去重复资讯入口 + 数据零态 + 资讯摘要/分类
- PaymentScreen:二维码/担保诚实化(去假扫码与夸大);保留原作者"本地过期不阻断支付"设计
- Products(商户):限时折扣价校验(整数分<售价/起<止)+ 编辑锁类型 + 列表搜索筛选
- Input(设计系统):密码显隐眼睛 — 全站密码框受益
- 异常日志:中文标签 + 语义着色(提现打款=绿而非误读告警)
- 补缺失的 `Icons.Plus`(原会运行时崩)
- **vite 代理补 `/index/` 与 `/config`** — 此前 dev 下门户 platformStats/articles 与买家 config 未接通(门户数据现真显示 2/13/32)

## ✅ 已完成(batch2 + batch3,均 build 绿 + 零运行时错误)
- 商户投诉处理:状态筛选 + 多行回复 + 时间列/上次回复/裁决备注
- 平台配置:费率 0~1 校验 + 百分比换算 + 回填(金融高危)
- 商户审核:新建商户入口(api.createMerchant);邀请码字段后端不收→跳过
- 支付渠道:验签自测详情(后端仅返回 valid→退化为可读错误文案)
- 优惠券:有效期列 + 过期态派生 + 状态/类型/券码筛选
- 钱包提现:收款方式结构化(支付宝/银行卡/微信)+ 流水/状态筛选 + 二次确认(手续费试算缺后端费率→跳过)
- 投诉仲裁:裁决弹窗带证据上下文 + 驳回备注必填 + 退款默认不勾(分页缺后端 total→跳过)
- 商品(跨商户):类型四分(卡密/知识/资源/权益)+ 缺货红标 + 统计卡
- 卡密管理:CSV 导出(脱敏)+ 权益码 label
- 章节弹窗:加载失败态 + 删除二次确认(替代 window.confirm)
- 满减满折:生效/失效时间字段 + 有效期列 + 筛选(后端需补 start_at/end_at 落库)
- 店铺装修:主题选中即时预览(作用域内联,不污染全局)+ 联系方式校验

## ✅ 后端/全栈已补(本轮,composer test 437 绿)
- **统计卡全局聚合**:商户审核/跨商户订单/跨商户商品/投诉仲裁/提现 全部改后端聚合(status_counts/summary/pending_summary,只读SQL,金额走bcmath),翻页不再失真。
- **满减满折限时活动**:迁移加 start_at/end_at + bestPromotion 按时间窗生效 + controller 透参 + 3 时间窗测试(TDD),过期/未开始活动不再误减。
- **图片上传**:UploadService(类型/大小校验+随机名存 public/uploads,4 单测)+ merchant.Upload 接口 + 前端 ImageUpload 组件接入商品主图/店铺图;端到端实测通过。
- **资源"重新获取下载链"**:实为前端即可(链每次查单现签发),加「刷新下载链」按钮。
- **优惠券规则交叉校验**:折扣 1~99、满减减额≤门槛。

## ⛔ 仍需你拍板(产品决策,我不擅自定 —— 涉及钱/集成)
- **部分退款**:现为全额退。是否允许部分退款?涉及退款金额输入 + 资金冲回口径(金额路径,需你定政策我再 TDD 实现)。
- **多支付渠道(微信/支付宝/USDT)**:现后端仅 epay 单聚合。补真实渠道需要各渠道商户号/密钥与对接,属业务+基建,需你提供。
- **权益类"已使用/已过期"态**:需定义权益码核销模型(平台是否跟踪权益码使用?如何标记?),是新功能,需你定设计。
- **真实支付二维码**:epay 返回的是收银台跳转 URL;能否渲染成可扫码 QR 取决于 epay 形态,需你确认是否值得做。
- **数据口径(演示)**:佣金 0% 是 SeedDemo 配的,非 bug;要 demo 显示真实数字需设非 0 佣金率并回填历史订单(纯演示美化,低优)。

## 🎨 低优先级打磨(可选,记录在案)
- 列表「删除」实心红满列 → 克制样式;刷新按钮 variant 跨页统一;Pill 加状态圆点
- 原生 select / datetime-local 统一为自绘组件
- 移动端宽表卡片化(订单/优惠券/分类等 7+ 列)
- 分类排序并列 0 → 拖拽/上下移/默认末位+1
- 销量/上新依赖的字段(created_at/sales_count)需后端补,否则隐藏对应排序项
